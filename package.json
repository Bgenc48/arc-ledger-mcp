{
  "name": "arc-ledger-mcp",
  "version": "0.15.1",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "description": "Arc & Ledger Tax Help - public, no-auth remote MCP server (Cloudflare Worker, Streamable HTTP)",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "bundle-check": "wrangler deploy --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:directory:live": "node scripts/verify-directory-live.mjs",
    "gen:products": "node scripts/gen-products-feed.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  },
  "overrides": {
    "@hono/node-server": "2.0.12",
    "fast-uri": "3.1.4"
  }
}
